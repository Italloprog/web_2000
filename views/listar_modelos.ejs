<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patro-Cars</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
     integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/index.css">
</head>
<body class="container d-flex flex-column min-vh-100">
       
    <header class="row">
        <h1><a href="/index">Patro-Cars</a></h1>
        <h2>listar todos os modelos </h2>
            <% if(montadora){ %>
                <h2> Montadora: <%= montadora.nome %></h2>
                <% } %>
    </header> 
      
    <main class="row flex-grow-1">

        <div class="col-md-6">
            <ul>
            <% for(let modelo of modelos){ %>
                <li>
                    <div><%= modelo.nome %> -  <%= modelo.valor_ref %></div>
                    <% if(montadora){ %>
                        <div>
                            <a href="/modelos_delete/<%= modelo.id %>/<%= montadora.id %>"><button>remover</button></a>
                            <a href="/modelos_editar/<%= modelo.id %>/<%= montadora.id %>"><button>editar</button></a>
                            <a href="/modelos_detalhe/<%= modelo.id %>"><button>detalhes</button></a>
                        </div>
                    <% }else{ %>
                    <div>
                        <a href="/modelos_delete/<%= modelo.id %>/0"><button>remover</button></a>
                        <a href="/modelos_editar/<%= modelo.id %>/0"><button>editar</button></a>
                        <a href="/modelos_detalhe/<%= modelo.id %>"><button>detalhes</button></a>
                    </div>
                    <% } %>
                    
                </li>
            <% } %>
            </ul>
        </div>
        
        <div class="col-md-6">
            <% if(montadora){ %>
                <a href="/form_modelos/<%= montadora.id %>">
                    <button>adicionar novo modelo</button>
                </a>
            <% }else{%>
                <a href="/form_modelos/0">
                    <button>adicionar novo modelo</button>
                </a>
            <% } %>
            
        </div>
        
       
    </main>
       
    <footer class="row">
        <p>maybe be cool</p>
    </footer>
</body>
</html>