<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patro-Cars</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
     integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/index.css">
</head>
<body class="container d-flex flex-column min-vh-100">
       
    <header class="row">
        <h1><a href="/index">Patro-Cars</a></h1>
        <h2>adicionar montadoras</h2>
    </header> 
      
    <main class="row flex-grow-1">

        <div class="col-md-10">
            <input type="text" id="nome" placeholder="Nome da montadora">
            <input type="text" id="pais" placeholder="Pais da montadora">
            <input type="number" id="ano" placeholder="Ano da montadora">
            <button id="enviar">Salvar</button>
        </div>
        
       
    </main>
       
    <footer class="row">
        <p>maybe be cool</p>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded',function(){
            document.getElementById('enviar').addEventListener('click',salvar)
        })

        async function salvar(){
            let nome = document.getElementById('nome').value
            let pais = document.getElementById('pais').value
            let ano = document.getElementById('ano').value

            let json = {
                nome : nome,
                pais : pais,
                ano : ano
            }

            let response = await fetch('http://localhost:3000/montadoras_save', {
                method: 'POST',
                body: JSON.stringify(json),
                headers: {
                'Content-type': 'application/json; charset=UTF-8',
                },
            })

            if (response.ok) {
                // Redirecionar para a página /listar após a requisição ser bem-sucedida
                window.location.href = '/listar_montadoras';
            } else {
                // Caso a requisição falhe, você pode exibir uma mensagem de erro
                alert('Falha ao salvar a montadora.');
            }
        }
    </script>
</body>
</html>